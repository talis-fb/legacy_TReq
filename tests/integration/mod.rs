#[cfg(test)]
mod file_handler {
    use treq::base::os::handler::FileHandler;

    #[test]
    #[ignore]
    fn sgodasdasdsa() {
        let _empty = FileHandler::default();
        assert_eq!(4, 4)
    }
}

#[cfg(test)]
mod network {
    #[test]
    #[ignore]
    fn should_request() {
        assert_eq!(1, 1)
    }
}
